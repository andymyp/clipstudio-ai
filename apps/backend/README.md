# Backend application entry point

The production backend implementation lives in the repository-level `backend/`
package. This directory is reserved as the deployable application boundary and
will receive environment-specific launch manifests in a later phase.
