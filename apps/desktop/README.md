# Desktop application skeleton

This is the Tauri/React application shell. It currently provides navigation,
an API client boundary, and placeholder pages. Native Tauri commands and full
UI modules are introduced by later prompts.

Install frontend dependencies with pnpm when it is available, then run:

```powershell
pnpm install
pnpm dev
```
